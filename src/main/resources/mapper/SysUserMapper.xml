<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="linecomment.music.mapper.SysUserMapper">
    <insert id="addUser" parameterType="linecomment.music.entities.dao.SysUser"  >
        INSERT INTO sys_user(user_id, nickname, gender, email, phone, birthday, avatar_url, code, create_time, login_time, description, login_ip)
        values (#{userId},#{nickname},#{gender},#{email},#{phone},#{birthday},#{avatarUrl},#{code},#{createTime},#{loginTime},#{description},#{loginIp})
    </insert>
</mapper>